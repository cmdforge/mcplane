import type { Command } from "commander";
import type { AppServices } from "../services.js";

export function registerRegistryCommands(_program: Command, _services: AppServices) {
  // Reserved for future registry command wiring.
}
